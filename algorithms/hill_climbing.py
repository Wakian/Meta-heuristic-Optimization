class HillClimbing:
    def __init__(self, f, domain, eps, max_it):
        self.f = f
        self.domain = domain
        self.eps = eps
        self.max_it = max_it

    def search(self):
        pass  # TODO
